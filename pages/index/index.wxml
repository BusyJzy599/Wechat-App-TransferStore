<!--index.wxml-->
<scroll-view scroll-y="true" style="height:100%" bindscrolltoupper="upper" bindscrolltolower="lower" bindscroll="scroll">
	<view class="top">
		<cover-view class="cover-top">
			hello!{{userInfo.nickName}}
		</cover-view>
		<image src="/pages/img/top.jpg" mode="heightFix"></image>
	</view>
	<view class="main">
		<!--搜索-->
		<view class="searchView">
			<van-search value="{{ value }}" shape="round" background="#FFEC8B" placeholder="搜索" />
		</view>
		<!--功能按钮-->
		<van-grid column-num="4">
			<van-grid-item icon="photo-o" text="文字" wx:for="{{ 4 }}" />
		</van-grid>

		<!--轮播
		<swiper class='u-wrp-bnr' indicator-dots='true' autoplay='true' interval='5000' duration='1000' circular='true'>
			<block wx:for="{{bnrUrl}}" wx:for-index="index">
				<swiper-item>
					<image src='{{item.url}}' class='u-img-slide' mode='aspectFill'></image>
				</swiper-item>
			</block>
		</swiper>
-->
		<!--主界面-->
		<view>
			<van-row>
				<van-col span="24">
					<van-divider contentPosition="center" textColor="black" fontSize="20">与您相关</van-divider>
					<!--信息-->
					<van-collapse value="{{ activeNames }}" bind:change="onCollapse">
						<van-collapse-item title="派送中" name="1">
							<van-skeleton title avatar row="3" loading="{{ loading }}">
							<title>111</title>
							<view>1234564444444444444444444444444444444444444444444444444444444</view>
							</van-skeleton>
						</van-collapse-item>
						<van-collapse-item title="您的订单" name="2">
						
						</van-collapse-item>
					</van-collapse>

				</van-col>
			</van-row>

		</view>
		<!--
<view class="container">
    <view class="userinfo">
      <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
      <block wx:else>
        <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
        <text class="userinfo-nickname">我爱你{{userInfo.nickName}}</text>
        <input bli value="{{1+1}}"/>
        <text>{{index.addTest(motto)}}</text>
        <view wx:for="{{student}}" wx:for-item="n" wx:for-index="i" wx:key="id">
          <text>{{i}}</text>
          <text>{{n.name}}</text>
        </view>
        <button bindtap="tabHandle" data-id="1" id="123">click</button>
      </block>
    </view>
    <view class="usermotto">
      <text class="user-motto">我{{motto}}</text>
      <van-button type="primary">按钮</van-button>
    </view>
  </view>
  -->

	</view>
</scroll-view>


<!--微信js代码，视图层逻辑 -->
<wxs module="index">
	module.exports = {
		addTest: function(input) {
			return input + '-test'
		}

	}
</wxs>